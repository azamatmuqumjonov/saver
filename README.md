установка компанентов
```
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.2/install.sh | bash
source ~/.bashrc
nvm install 22
nvm use 22
npm i -g pm2
npm install node-telegram-bot-api axios
```
